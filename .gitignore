# Terraform state — never commit, use remote state instead
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# Variable overrides that may contain secrets
*.tfvars
*.tfvars.json

# Crash logs
crash.log
crash.*.log
